---
phase: quick
plan: 260319-vv7
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/sidebar.tsx
  - src/components/first-sync-banner.tsx
  - src/app/page.tsx
  - src/app/sessions/page.tsx
  - src/app/projects/page.tsx
  - src/app/costs/page.tsx
autonomous: true
requirements: [first-sync-empty-state]
must_haves:
  truths:
    - "Sidebar shows 'Initial sync in progress...' with animated indicator when isRunning && sessionCount===0"
    - "Overview, Sessions, Projects, and Costs pages show a first-sync banner when DB is empty and ingest is running"
    - "Banner disappears automatically once sync completes and data appears (SWR revalidation)"
    - "Pages still show their normal loading spinners during fetch, then banner if data is empty + sync running"
  artifacts:
    - path: "src/components/first-sync-banner.tsx"
      provides: "Reusable first-sync empty state banner component"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Updated sidebar with first-sync state"
  key_links:
    - from: "src/components/first-sync-banner.tsx"
      to: "useSyncStatus"
      via: "import from @/lib/hooks"
      pattern: "useSyncStatus"
---

<objective>
Add first-sync empty state UI so users see clear feedback when the database is syncing for the first time instead of confusing empty/zero-data screens.

Purpose: On first launch after v1.1 upgrade, ingest takes time. Users need to know data is loading, not missing.
Output: A reusable banner component and updated sidebar state indicator.
</objective>

<execution_context>
@/home/rfelix/.claude/get-shit-done/workflows/execute-plan.md
@/home/rfelix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/hooks.ts
@src/components/layout/sidebar.tsx
@src/app/page.tsx
@src/app/sessions/page.tsx
@src/app/projects/page.tsx
@src/app/costs/page.tsx

<interfaces>
<!-- From src/lib/hooks.ts -->
```typescript
export interface SyncStatus {
  lastSynced: string | null;
  sessionCount: number;
  isRunning: boolean;
}

export function useSyncStatus() {
  return useSWR<SyncStatus>('/api/sync-status', fetcher, {
    refreshInterval: 5000,
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FirstSyncBanner component and update sidebar</name>
  <files>src/components/first-sync-banner.tsx, src/components/layout/sidebar.tsx</files>
  <action>
**FirstSyncBanner (new file src/components/first-sync-banner.tsx):**

Create a 'use client' component that calls `useSyncStatus()` internally and renders conditionally:
- Show banner ONLY when `syncStatus?.isRunning === true && syncStatus?.sessionCount === 0`
- If condition is false, render `null` (no wrapper div, no layout impact)
- Banner design: use Card with `border-primary/30 bg-primary/5` styling. Inside:
  - A spinning indicator (reuse the `h-4 w-4 animate-spin rounded-full border-2 border-primary border-t-transparent` pattern)
  - Heading: "Database is syncing for the first time" (text-sm font-semibold)
  - Subtext: "Historical data will appear here shortly. Active sessions are available now." (text-xs text-muted-foreground)
  - A Link to /active page: "View active sessions" styled as text-xs text-primary hover:underline
- Import from: Card (ui/card), Link (next/link), useSyncStatus (hooks), Database icon (lucide-react)

**Sidebar update (src/components/layout/sidebar.tsx):**

In the bottom status section, update the fallback else branch (line 91-94). Currently it shows "Syncing..." or "Reading from ~/.claude/". Change the logic:
- When `syncStatus?.isRunning && syncStatus?.sessionCount === 0`: show "Initial sync in progress..." with a small animated dot (use `animate-pulse` on a small primary-colored dot span). Below it show "Scanning ~/.claude/" in text-[10px] text-muted-foreground.
- When `syncStatus?.isRunning && sessionCount > 0 && !lastSynced`: show "Syncing..." (existing behavior)
- Else (not running, no lastSynced): show "Reading from ~/.claude/" (existing behavior)

Keep all existing imported state (syncStatus, sourceInfo) and the existing `isImported` and `lastSynced` branches unchanged.
  </action>
  <verify>
    <automated>cd /mnt/c/SourceControl/GitHub/Claud-ometer && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>FirstSyncBanner component exists and renders nothing when sync is complete or not running. Sidebar shows distinct "Initial sync in progress..." message when isRunning && sessionCount===0.</done>
</task>

<task type="auto">
  <name>Task 2: Add FirstSyncBanner to all historical pages</name>
  <files>src/app/page.tsx, src/app/sessions/page.tsx, src/app/projects/page.tsx, src/app/costs/page.tsx</files>
  <action>
Add `import { FirstSyncBanner } from '@/components/first-sync-banner';` to each of the four pages.

**For each page, place `<FirstSyncBanner />` immediately after the page title/subtitle div, before the data content.** The banner self-manages visibility via useSyncStatus so no conditional logic needed in pages.

Specific insertion points:

- **page.tsx (Overview):** After the `<div>` containing `<h1>Overview</h1>` (line 39-42), before the Hero Stats grid (line 44). Add `<FirstSyncBanner />` between them.

- **sessions/page.tsx:** Inside `SessionsContent`, after the header div (line 68-76), before the search input div (line 78). Add `<FirstSyncBanner />` between them.

- **projects/page.tsx:** After the header div (line 25-29), before the grid (line 31). Add `<FirstSyncBanner />` between them.

- **costs/page.tsx:** After the header div (line 77-80), before the stat grid (line 82). Add `<FirstSyncBanner />` between them.

No other changes to these files. The banner handles its own state and renders null when not needed.
  </action>
  <verify>
    <automated>cd /mnt/c/SourceControl/GitHub/Claud-ometer && npx tsc --noEmit --pretty 2>&1 | head -30 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All four historical pages include FirstSyncBanner. Build succeeds. Banner shows only during first sync (isRunning && sessionCount===0) and auto-hides via SWR revalidation once data arrives.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` succeeds
3. Visual check: when DB is empty and ingest is running, sidebar shows "Initial sync in progress..." and pages show the banner
4. Visual check: once sync completes, banner disappears and sidebar shows normal "Synced X ago" state
</verification>

<success_criteria>
- FirstSyncBanner component exists and is self-contained (calls useSyncStatus internally)
- Sidebar distinguishes first-sync state from general syncing state
- All 4 historical pages (overview, sessions, projects, costs) show first-sync banner
- Banner auto-dismisses when data appears (no manual refresh needed)
- TypeScript compiles, build passes
</success_criteria>

<output>
After completion, create `.planning/quick/260319-vv7-add-first-sync-empty-state-ui-sidebar-sh/260319-vv7-SUMMARY.md`
</output>
